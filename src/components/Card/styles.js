import { css } from 'styled-components';

const card = css`
    width: 300px;
    margin: 0 30px;
    overflow: hidden;

    img {
        width: auto;
        height: 280px;
        margin: 0 auto;
    }

    .ant-card-meta-title {
        font-size: 24px;
    }
`

export default card;
